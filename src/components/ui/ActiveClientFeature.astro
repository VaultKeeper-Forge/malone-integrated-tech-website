---
import type { FeaturedWork } from '../../data/siteData';
import FeaturedWorkFeature from './FeaturedWorkFeature.astro';

interface Props {
  client: FeaturedWork;
}

const { client } = Astro.props;
---

<FeaturedWorkFeature work={client} />
